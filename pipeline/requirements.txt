# DoseCheck pipeline zavisnosti
# PubMed korak (pubmed.py, rank.py) koristi SAMO standardnu biblioteku — radi bez instalacije.
anthropic>=0.40.0        # AI destilacija (distill.py)
psycopg2-binary>=2.9     # upis u Supabase/Postgres (db_upload.py)
python-dotenv>=1.0       # učitavanje .env (opciono, radi i bez)
